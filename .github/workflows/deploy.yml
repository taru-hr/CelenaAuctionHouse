name: Deploy site

# Publishes the static site to GitHub Pages on CODE changes (pushes to main).
# Hourly DATA updates deploy themselves from update-data.yml, so this no longer
# relies on a workflow_run hand-off (which was leaving the live site stale).

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Checkout (app)
        uses: actions/checkout@v5

      - name: Assemble site
        run: |
          mkdir -p _site
          rsync -a \
            --exclude '.git' --exclude '.github' --exclude '.claude' \
            --exclude 'node_modules' --exclude 'scripts' --exclude 'dist' --exclude '_site' \
            --exclude 'webpack.*.js' --exclude 'webpack.common.js' \
            --exclude 'package.json' --exclude 'package-lock.json' \
            --exclude '.editorconfig' --exclude '.gitattributes' --exclude '.gitignore' \
            ./ _site/
          # Overlay the live data branch (falls back to the sample data in the repo).
          if git ls-remote --exit-code --heads origin data >/dev/null 2>&1; then
            git fetch --depth=1 origin data
            rm -rf _site/data && mkdir -p _site/data
            git archive FETCH_HEAD | tar -x -C _site/data
            echo "Deployed live data ($(ls _site/data/history | wc -l) series)."
          else
            echo "No data branch yet — deploying with sample data."
          fi
          touch _site/.nojekyll

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deploy
        uses: actions/deploy-pages@v4
